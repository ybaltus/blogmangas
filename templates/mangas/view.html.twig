{% extends 'base.html.twig' %}

{% block title %}Manga: {{ manga.title }}{% endblock %}

{% block body %}
    <div class="jumbotron jumbotron-fluid text-right">
        <div class="container">
            <h1 class="display-4">{{manga.title}}</h1>
            <h6 class="text-muted">{{ manga.typeName|capitalize }} - {{ manga.year }}</h6>
            <p>{{ manga.author }}</p>
            <p>{{ manga.country }}</p>
        </div>
    </div>
    <div class="container">
        <div class="row">
            <h2>Description: </h2>
            <p class="text-justify">{{ manga.description }}</p>
        </div>
        <div class="row">
            <div class="col-md-6 col-sm-6 col-xs-12">
                <h3>Épisodes</h3>
                <table class="table table-dark table-sm">
                    <thead>
                    <tr>
                        <th scope="col">#</th>
                        {% if manga.animes|length >0  %}
                            {% for anime in manga.animes %}
                                <th scope="col">Saison {{ anime.season }}</th>
                            {% endfor %}
                        {% else %}
                            <th scope="col">Aucune saison</th>
                        {% endif %}
                    </tr>
                    </thead>
                    <tbody>
                    <tr>
                        <th scope="row">Épisodes</th>
                        {% if manga.animes|length > 0 %}
                            {% for anime in manga.animes %}
                                <td>{{ anime.currentEpisode }} / {{ anime.maxEpisode }}</td>
                            {% endfor %}
                        {% else %}
                            <td>Aucun épidode</td>
                        {% endif %}
                    </tr>
                    </tbody>
                </table>
            </div>
            <div class="col-md-6 col-sm-6 col-xs-12">
                <h3>Chapitres</h3>
                <table class="table table-dark table-sm">
                    <tbody>
                    <tr>
                        <th scope="row">Chapitres</th>
                        {% if manga.scans %}
                            <td>{{ manga.scans.currentChapter }} / {{ manga.scans.maxChapter }}</td>
                        {% else %}
                            <td>Aucun</td>
                        {% endif %}
                    </tr>
                    </tbody>
                </table>
            </div>

        </div>
        <div class="row">
            <figure class="col-md-3 col-sm-6 col-xs-12 figure">
                <img src="..." class="figure-img img-fluid rounded" alt="...">
                <figcaption class="figure-caption text-right">Photo 1</figcaption>
            </figure>
            <figure class="col-md-3 col-sm-6 col-xs-12 figure">
                <img src="..." class="figure-img img-fluid rounded" alt="...">
                <figcaption class="figure-caption text-right">Photo 2</figcaption>
            </figure>
            <figure class="col-md-3 col-sm-6 col-xs-12 figure">
                <img src="..." class="figure-img img-fluid rounded" alt="...">
                <figcaption class="figure-caption text-right">Photo 3</figcaption>
            </figure>
        </div>
    </div>
{% endblock %}